Megan Massey: mem2318@columbia.eduJeremy Grill: jeg2205@columbia.eduEthan O'Neal: ejo2117@columbia.eduTo test the implementation of the compiler, run the following command:$ make runRunning this does the following: It builds toplevel.native through the command:ocamlbuild -use-ocamlfind -pkgs llvm,llvm.analysis -cflags -w,+a-4 toplevel.nativeThen runs the toplevel executable on the hello.mus test file, outputting the resulting llvm form in hello.ll./toplevel.native tests/hello.mus > tests/hello.llTo test the output of the compiler against a gold standard, run $ chmod +x testhello.sh$ ./testhello.sh